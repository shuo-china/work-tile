export enum StoreId {
  App = 'app-store',
  User = 'user-store',
  Menu = 'menu-store',
  ProjectMenu = 'project-menu-store'
}

export enum LayoutMode {
  Side = 'side',
  Top = 'top',
  Mix = 'mix'
}
